[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = ">=2.2.0"
torchvision = ">=0.17.0"
torchaudio = ">=2.2.0"
numpy = ">=1.26.0"
pandas = ">=2.2.0"
scikit-learn = ">=1.5.0"
matplotlib = ">=3.8.0"
tqdm = ">=4.66.0"
opencv-python = ">=4.9.0"
scikit-image = ">=0.24.0"

[dev-packages]

[requires]
python_version = "3.11"
